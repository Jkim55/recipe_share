#recipes.transitions-enabled
  - @recipe.each do |r|
    .box.panel.panel-default
      = link_to (image_tag r.image.url), r
      .panel-body
        %h2= link_to r.title, r
        %p.user
        ICONS
        -# -#  quick & easy
        -# %span.glyphicon.glyphicon-stopwatch
        -# -# vegetarian
        -# %span.glyphicon.glyphicon-flower
        -# -# pescatarian
        -# %span.glyphicon.glyphicon-fishes
        -# -# breakfast, lunch, dinner, or brunch
        -# %span.glyphicon.glyphicon-dining-set
        -# -# appetizer or side
        -# %span.glyphicon.glyphicon-plus
        -# -# dessert
        -# %span.glyphicon.glyphicon-cake
